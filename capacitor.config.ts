import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.allindiaaccountable.app',
  appName: 'allindiaaccountable',
  webDir: 'out'
};

export default config;
